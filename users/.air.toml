# .air.toml — Air config for hot reload
root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/users-service ./cmd/*.go"
bin = "tmp/users-service"
full_bin = ""
include_ext = ["go"]
exclude_dir = ["tmp", "vendor"]

[log]
timestamp = true

